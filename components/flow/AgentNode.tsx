// Deprecated: AgentNode was used by the old ReactFlow-based flow page.
// The new flow page uses ActiveChainView + SystemOverview instead.
// Kept as empty export to avoid import errors if referenced elsewhere.
export default function AgentNode() {
  return null
}
